using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    private string _Name = "Marcin";

    // Start is called before the first frame update
    void Start()
    {
        print("Hello "+ _Name);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
